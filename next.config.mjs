/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['github.com', 'pbs.twimg.com'], // Add domains for external images
      },
};

export default nextConfig;
